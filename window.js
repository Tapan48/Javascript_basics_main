// window is a browsers object and is automatically created by browser

// window is a global object with lots of properties and methods  which represents the browser window and provides access to the browser's functionality.

// window is a parent object of all the objects in javascript

console.log("this is window.document object", window.document); /// this is DOM  --->it is a tree of objects representing the html document

//// on page load browser creates a----> window object ----> document(DOM) ----> html ----> body ----> div ----> h1 ----> text(tree of objects representing the html document)

//when a page is loaded, the browser creates a window object and a document object

// when a page is loaded, the browser creates a window object with a document object

// console.dir(window.document);
// console.log(window.document.body);
// console.dir(window.document.body);
// console.log(window.document.body.childNodes);

